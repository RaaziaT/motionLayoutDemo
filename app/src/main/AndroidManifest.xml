<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cloudasset.motionlayout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DraggableGridView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".rnd.TabedActivity"
            android:label="@string/title_activity_tabed"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".walkthrough.WalkThroughActivity">

        </activity>
        <activity
            android:name=".zapanimation.AnimationActivity"
            android:theme="@style/AppThemeB" />
        <activity
            android:name=".rnd.MainActivityB"
            android:label="@string/title_activity_main_b"
            android:theme="@style/AppThemeB" />
        <activity android:name=".productdetails.MainActivity" />
        <activity android:name=".productdetails.MainActivityJava" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>